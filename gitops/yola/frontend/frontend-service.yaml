apiVersion: v1
kind: Service
metadata:
  name: yola-frontend
  namespace: yola
  labels:
    app: yola-frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: yola-frontend
  type: ClusterIP
