# ── Секреты и ключи (НЕ УДАЛЯТЬ) ──
k8s/secrets.yml
gitops/external-secrets/azure-sp-secret.yaml
# Cosign private key — NEVER commit this
gitops/argocd/cosign.key

# ── Terraform (НЕ УДАЛЯТЬ) ──
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate*
terraform/.tfvars
terraform/terraform.tfvars
terraform/crash.log
terraform/crash.*.log
terraform/*.tfstate
terraform/*.tfstate.*
terraform/**/.terraform/
# Local .terraform directories
**/.terraform/*
# .tfstate files
*.tfstate
*.tfstate.*
# Crash log files
crash.log
crash.*.log

# ── Зависимости и билды (Фикс зависания Git) ──
# Двойная звездочка (**/) игнорирует эти папки ВЕЗДЕ в проекте
**/node_modules/
**/dist/
**/build/
**/.next/
**/out/

# ── Инфраструктура и локальные данные кластера ──
yola-data/
*.log

# ── Claude / AI agent files ──
.claude/
CLAUDE.md
AGENTS.md
.DS_Store
